# 更新日志

## v3.0.0（2026-08-07）
### ✨ 新功能
- **语音对话（免按键 + 唤醒词）**：右键 →「语音对话」开启后无需按键，说唤醒词（默认「yumi」）唤醒，之后连续语音对话，回复自动 TTS 朗读
  - 基于 VAD 静音自动断句：说完一句停顿片刻（默认 0.8 秒）即自动识别，长句不再被固定时间窗截断；断句时长可调
  - 修复唤醒无响应：连续监听改为由 `text()` 驱动 VAD 循环（库在首次 `text()` 调用后才武装自动断句），说唤醒词后停顿片刻即唤醒
  - 修复无法关闭：停止监听不再调用会卡死的 `abort()`（改为仅静音麦克风），识别超时中断放独立线程；启动失败自动复位状态；开启「语音输入」时会自动先关闭语音对话，不再卡在「请先关闭语音对话」
  - 修复唤醒无效：唤醒等待改为“持续监听不切片”，不再每 2 秒打断录音（之前唤醒词说到一半会被切片中断吃掉）；只在空闲超时或关闭时中断等待
  - 开启语音对话自动启动 Soullink：回复走 Soullink 的表情 + TTS 管线（Soullink 未就绪前先用独立 TTS 兜底）；朗读结束通过 JS 回调精确恢复麦克风，避免自听自说；关闭语音对话时若 Soullink 是自动开启的会一并关闭

### 🔧 修复
- **主动发言朗读模式**：TTS 朗读时不显示文字气泡、不读括号内容（Soullink 或独立 TTS）；未启用 TTS 时保留文本模式气泡显示原文
- **主动发言不再受聊天模式限制**：聊天框开着时也会照常主动发言（去掉 `chat_mode` 限制）；同时修复设置对话框未启动计时器的问题
- **实时语音对话不显示文字气泡**：Soullink 朗读与兜底朗读都不再弹回复气泡（仅语音对话），打字聊天气泡保持原样 6 秒；其他逻辑不变
- **黑脸不还原**：表情反应计时器不再被连续反应无限重置（普通聊天黑脸 4 秒硬上限，其余 8 秒）；Soullink 同一情绪 2.5 秒内重复触发时跳过表情反应（TTS 不受影响）；情绪关键词去掉「烦」「怒」「哼」等过宽单字，降低「麻烦/哼」等日常用语的误判

### ✨ 新功能
- **每个模型可自定义 Soullink 情绪动作**：右键 →「Soullink 情绪引擎」→「选择情绪动作…」，为开心/好奇/疑惑/累/生气等 16 种情绪分别指定动作姿势（如 累 → 看手机）；写入 profile 后立即生效，重新生成 profile 时通过生成器保留
- **每个模型可自定义 Soullink 待机动作**：右键 →「Soullink 情绪引擎」→「选择待机动作姿势…」，勾选当前模型自带的姿势作为待机随机小动作（悠小喵的扶脸/看手机/记笔记/前倾、yumi 的拿话筒/抬手等均可），按模型保存在 `model.json`
- **Soullink 手部动作**：情绪映射加入扶脸（hand）/看手机（phone）——好奇、疑惑时扶脸思考，累的时候看手机；待机超过约 25 秒还会随机做扶脸/看手机/记笔记/前倾等小动作，几秒后自动恢复；模型没有对应表情时自动跳过
- **裁切显示**：右键菜单新增「裁切（从脚底往上）」，滑块按 0-90% 从脚底向上裁切显示区域（如裁 50% 只显示胸部以上），窗口自动适配，可一键重置
- **动作幅度设置**：Soullink 设置新增「动作幅度」（0.5x~2x），内置动作引擎与 Soullink SDK 共用
  - 内置引擎：待机改为明显的左右摇晃（身体 + 头部），思考/工作/生气/点头等姿态幅度统一按设置放大
  - Soullink：`bodyMotionGain` 不再写死，按幅度设置联动（默认 1x 保持原 2.8 强度）
  - 唤醒词/退出语可自定义，说「帮我看看」也会自动读屏幕再回答，记忆功能同步生效
  - 空闲超时或说「拜拜/退出对话」自动回到待唤醒状态；TTS 复用百炼 Key，未配置时只显示文字
- **聊天触发读屏幕**：对桌宠说「帮我看看」「看看屏幕」「你能看到我的屏幕吗」等话时，自动截屏识别画面，并把屏幕内容和你的问题一起发给模型回答；看到的内容也会记入短期记忆
- **本地记忆**：yumi 不再“聊完就忘”
  - **短期记忆**：最近几小时内的对话实时写入 `memory/short_term.jsonl`，重启桌宠后自动恢复，能接上之前的聊天
  - **长期记忆**：每轮对话后用聊天模型自动提炼值得记住的事实（姓名、喜好、约定、最近关心的事等），合并去重、按重要度/新鲜度保留上限，存入 `memory/long_term.json`
  - **语义召回**：每次回复前按当前话题检索最相关的长期记忆注入提示词（Embedding 语义检索，未配置 Key 时自动降级为本地关键词匹配），主动发言和读屏回复也带记忆上下文
  - 所有对话（聊天/主动发言/读屏）以短期记忆为主、长期记忆按相关度补充（不相关不注入）；两者都没有则按人设回答，始终以人设为准
  - 右键菜单新增「记忆」子菜单：开关、设置（短期条数/时长、提炼开关、上限、语义检索接口）、查看、清空
  - 记忆数据全部保存在本机 `memory/` 目录，已加入 `.gitignore`，不随仓库分发

## v2.0.6（2026-08-07）

### ✨ 新功能

- **语音输入支持「按住说话」快捷键**：开启语音输入后，按住设定按键开始录音，松开自动识别并直接发送，不用再点发送按钮
  - 默认快捷键 F8，可在「语音输入设置」里改成 F1-F12 / Caps Lock 等，并可加 Ctrl / Shift / Alt / Win 组合键
  - 快捷键是全局的，不聚焦桌宠窗口也能用；需在聊天模式下使用
  - 首次加载模型时如果按键就松开了，会自动取消本次录音，不会空录

## v2.0.5（2026-08-07）

### 🔧 修复

- **修复语音识别模型下载失败**：此前模型由 RealtimeSTT 内部直接下载，卡死/失败时没有明确提示，且国内网络直连 HuggingFace 容易失败
  - 改为先显式下载模型再启动识别：按「ModelScope（国内）→ 配置镜像 → hf-mirror.com → huggingface.co」自动回退重试，带下载进度提示，失败会给出明确原因和解决办法
  - 补装 `silero-vad`，人声检测（VAD）也改为使用本地模型文件，不再需要访问 GitHub
  - 模型缓存到项目 `.cache/stt`，首次下载后离线可用，不会重复下载

## v2.0.4（2026-08-07）

### ✨ 新功能

- **主动发言**：桌宠不再只是被动等聊天。可以开启“主动发言”，她会在你忙别的事时主动说一两句关心/碎碎念（走聊天模型，未配置聊天接口时有兜底台词）
  - 不是固定节奏：按“平均间隔 + 开口概率”掷骰子决定时机，时间越久越容易开口，最短间隔兜底
  - 参数全部可调：平均间隔（分钟）、开口概率（%）、最短间隔（分钟），随时在右键菜单里开关和设置
- **语音输入**：聊天输入框左侧新增 🎤 按钮，点一下开始录音、再点一下结束并识别成文字填入输入框
  - 识别完全在本机完成（RealtimeSTT + faster-whisper），不上传任何音频，不依赖云端 API
  - 可设置识别模型（tiny/base/small/medium/large）与语言（中文/英文/日文/自动），首次使用会自动下载模型（small 约 460MB），之后离线可用
  - 下载不了模型时可填写国内镜像（如 https://hf-mirror.com）重试

## v2.0.3（2026-08-06）

### ✨ 优化

- **右键菜单美化**：从系统原生样式改为暗色玻璃质感——圆角、半透明深色背景、紫罗兰渐变高亮、柔和分隔线，顶部显示当前模型名；主菜单与所有子菜单统一风格

## v2.0.2（2026-08-06）

### ✨ 新功能

- **Soullink 情绪引擎现在能驱动原生表情了**：此前 pixi-live2d-display 只从 `model3.json` 的 `FileReferences.Expressions` 读取表情，而标准模型文件把它们放在顶层，导致 SDK 的原生表情从未加载成功
  - 悠小喵：注册了 星星眼 / 晕晕眼 / 黑脸 / 流泪 / 哭哭 / 脸红 / 飞头 等 12 个表情，情绪识别到 happy / sad / angry / surprised / shy / confused 等会直接切换对应脸
  - yumi：同样补上 `FileReferences.Expressions`，爱心眼 / 星星眼 / 眼泪 / 黑脸 / 蚊香眼 等原生表情生效
- **导入模型自动注册 Expressions**：导入新模型时，如果模型目录里有 `.exp3.json` 表情文件但 `model3.json` 未声明，会自动补注册（含星星眼的 happy 别名），情绪引擎开箱即用
- **profile 生成器自动补全情绪别名**：teary / anxiety / tired / concerned 归入 tear，anger 归入 angry，affectionate 归入 happy，curious 归入 star，confused 归入 surprised

## v2.0.1（2026-08-06）

### 🔧 修复

- **Soullink 模式下恢复鼠标追踪**：开启情绪引擎后角色不再跟随鼠标，现在眼神/头部/身体会继续跟随光标（幅度减半，避免完全盖掉 SDK 的情绪姿态）
- **修复回复时"只动嘴、身体没反应"**：SDK 在说话（SPEAKING）状态下会主动压低情绪层权重并关闭空闲手势，导致回复时身体几乎不动。通过提高参数/身体增益（parameterGain 2.2 / bodyMotionGain 2.8），情绪驱动的头部偏转、微笑、眼神等姿态在说话时也能清晰可见

## v2.0.0（2026-08-06）

### ✨ 新功能

- **读屏幕**：右键菜单「读屏幕」可开关定时观察屏幕，设置读取频率（默认 10 分钟一次），调用视觉大模型识别画面内容，再交给聊天模型按人设生成一句回应
- **Soullink 情绪引擎（可选开关）**：接入 [Soullink Emotion SDK](https://github.com/nanlingyin/soullink-emotion-sdk)
  - 情绪识别改用 **Embedding 分类**（不经过 LLM 生成文本）：内置 1,400 条中文语料、Top-K 投票、精确命中、LRU 查询缓存与规则降级，输出连续 VAD 情绪
  - 动作与表情改由 Soullink 引擎驱动（连续 VAD + FACS 表情 + 待机动作 + 说话口型），支持 natural / lively / calm / shy 动作风格
  - 聊天回复自动用 TTS 朗读（默认百炼 Qwen-TTS / Cherry 音色，可配置）
  - 该功能作为开关使用：**关闭时完全恢复原有关键词情绪识别和动作系统**
- **Soullink 与 Codex 联动互斥**：开启 Soullink 期间自动禁用 Codex 状态动作匹配（思考/执行/完成/故障不再抢动作），关闭后立即恢复
- **任意模型自动生成 profile**：开启 Soullink 时若当前模型没有 `soullink.profile.json`，自动扫描模型生成参数映射；导入新模型后也会自动补生成；菜单可手动「生成/更新当前模型 profile…」

### 🔧 修复

- 修复 `codex_monitor.py` 中时间戳解析函数实现错位导致的死代码问题（`_iso_to_epoch` 之前永远返回 `None`）
- 移除调试遗留的 `pet_diag.log` / `pet_state_ack.log` 写入逻辑与日志文件
- 移除废弃的 `pets/yumi/manifest.json` 读取路径（旧架构遗留，实际从未命中）

### 🧹 整理

- 清理 `_expr_classify` 中重复的关键词
- 补充 Soullink 集成所需文件：`soullink_runner.py`、`tools/soullink/`（Node 侧服务 + profile 生成 + 浏览器桥接）、`tools/web/lib/soullink-emotion.esm.js`（构建产物）
